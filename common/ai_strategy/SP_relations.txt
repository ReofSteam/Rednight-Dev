###DIPLOMACY AI###

##Factional Relations##

#British Empire and France#
british_empire_germany = {
	enable = {
		is_in_faction_with = GBR
		country_exists = GER
		#GER = { has_socialist_government = yes }
		#GBR = { NOT = { has_socialist_government = yes } }
	}
	abort = {
		OR = { #If one of the above conditions is no longer met, abort the strategy
			NOT = { is_in_faction_with = GBR }
			NOT = { country_exists = GER }
			#GER = { NOT = { has_socialist_government = yes } }
			#GBR = { has_socialist_government = yes }
		}
	}

	ai_strategy = {
		type = contain
		id = "GER"
		value = 100
	}
	ai_strategy = {
		type = conquer
		id = "GER"
		value = 100
	}
	ai_strategy = {
		type = antagonize
		id = "GER"
		value = 100
	}
}

british_empire_soviet_union = {
	enable = {
		is_in_faction_with = GBR
		country_exists = RUS
		#RUS = { has_socialist_government = yes }
		#GBR = { NOT = { has_socialist_government = yes } }
	}
	abort = {
		OR = { #If one of the above conditions is no longer met, abort the strategy
			NOT = { is_in_faction_with = GBR }
			NOT = { country_exists = RUS }
			#RUS = { NOT = { has_socialist_government = yes } }
			#GBR = { has_socialist_government = yes }
		}
	}

	ai_strategy = {
		type = contain
		id = "RUS"
		value = 100
	}
	ai_strategy = {
		type = conquer
		id = "RUS"
		value = 100
	}
	ai_strategy = {
		type = antagonize
		id = "RUS"
		value = 100
	}
}

france_germany = {
	enable = {
		is_in_faction_with = FRA
		country_exists = GER
		#GER = { has_socialist_government = yes }
		#FRA = { NOT = { has_socialist_government = yes } }
	}
	abort = {
		OR = { #If one of the above conditions is no longer met, abort the strategy
			NOT = { is_in_faction_with = FRA }
			NOT = { country_exists = GER }
			#GER = { NOT = { has_socialist_government = yes } }
			#FRA = { has_socialist_government = yes }
		}
	}

	ai_strategy = {
		type = contain
		id = "GER"
		value = 100
	}
	ai_strategy = {
		type = conquer
		id = "GER"
		value = 100
	}
	ai_strategy = {
		type = antagonize
		id = "GER"
		value = 100
	}
}

france_soviet_union = {
	enable = {
		is_in_faction_with = FRA
		country_exists = RUS
		#RUS = { has_socialist_government = yes }
		#FRA = { NOT = { has_socialist_government = yes } }
	}
	abort = {
		OR = { #If one of the above conditions is no longer met, abort the strategy
			NOT = { is_in_faction_with = FRA }
			NOT = { country_exists = RUS }
			#RUS = { NOT = { has_socialist_government = yes } }
			#FRA = { has_socialist_government = yes }
		}
	}

	ai_strategy = {
		type = contain
		id = "RUS"
		value = 100
	}
	ai_strategy = {
		type = conquer
		id = "RUS"
		value = 100
	}
	ai_strategy = {
		type = antagonize
		id = "RUS"
		value = 100
	}
}

#Internationale#
comintern_england = {
	enable = {
		is_in_faction_with = RUS
		country_exists = GBR
		#RUS = { has_socialist_government = yes }
		#GBR = { NOT = { has_socialist_government = yes } }
	}
	abort = {
		OR = { #If one of the above conditions is no longer met, abort the strategy
			NOT = { is_in_faction_with = RUS }
			NOT = { country_exists = GBR }
			#RUS = { NOT = { has_socialist_government = yes } }
			#GBR = { has_socialist_government = yes }
		}
	}

	ai_strategy = {
		type = contain
		id = "GBR"
		value = 100
	}
	ai_strategy = {
		type = conquer
		id = "GBR"
		value = 100
	}
	ai_strategy = {
		type = antagonize
		id = "GBR"
		value = 100
	}
}

comintern_france = {
	enable = {
		is_in_faction_with = RUS
		country_exists = FRA
		#RUS = { has_socialist_government = yes }
		#FRA = { NOT = { has_socialist_government = yes } }
	}
	abort = {
		OR = { #If one of the above conditions is no longer met, abort the strategy
			NOT = { is_in_faction_with = RUS }
			NOT = { country_exists = FRA }
			#RUS = { NOT = { has_socialist_government = yes } }
			#FRA = { has_socialist_government = yes }
		}
	}

	ai_strategy = {
		type = contain
		id = "FRA"
		value = 100
	}
	ai_strategy = {
		type = conquer
		id = "FRA"
		value = 100
	}
	ai_strategy = {
		type = antagonize
		id = "FRA"
		value = 100
	}
}