scripted_gui = {

	PARDAN_status = {
        context_type = player_context
		parent_window_token = top_bar
		window_name = "PARDAN_status"
		
		triggers = {
        }
        visible = {
        always = yes
        }
    }
 }